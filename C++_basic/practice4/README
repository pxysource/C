                       第四部分 ∙ 知识点：泛型
【4.1】编写一个函数模板，返回两个参数中的较大值。（函数模板）
【4.2】编写一个模板函数 maxn( )，使之接收一个类型为T的数组和数组的元素个数，返回
数组中最大的元素。并设计一个具体化版本，可接收类型为 char * 的数组，返回的是最长
的字符串。（函数模板、模板重载、函数模板全特化）
【4.3】编写一个表示栈的模板类，并用栈实现以下功能：（类模板及其偏特化、全特化）
1. 利用栈，实现十进制转成八进制和十六进制
2. 利用栈，实现加减乘除四则运算
